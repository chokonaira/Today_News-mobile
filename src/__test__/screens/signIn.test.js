import React from "react";
import { render, fireEvent } from "react-native-testing-library";
import SingIn from "../../screens/SignIn";

describe("", () => {
  it("throw error message when the input is invalid for firstname", () => {
    // const tree = render(<SingIn/>);
    console.log("Hello");
  });
});
